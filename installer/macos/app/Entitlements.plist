<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the AIFX installer.

  Notarisation requires the hardened runtime AND a signed bundle. The
  installer does plain filesystem I/O to a user's OFX plugin directory
  and shells out to /usr/bin/xattr — nothing exotic. No exemptions are
  needed at this time. Add new entitlements here only if a future build
  actually requires them (e.g. for JIT, library validation off, etc.).
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>
    <key>com.apple.security.cs.allow-jit</key>
    <false/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <false/>
</dict>
</plist>
